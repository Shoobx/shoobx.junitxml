[run]
branch = True
include=src/shoobx*
omit=*/test*

[report]
# Regexes for lines to exclude from consideration
exclude_lines =
    # Have to re-enable the standard pragma
    pragma: no cover
    def __repr__

[paths]
source =
    src/
